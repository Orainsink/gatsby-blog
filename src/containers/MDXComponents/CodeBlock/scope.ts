import React, {
  useEffect,
  useState,
  useRef,
  useCallback,
  useMemo,
} from 'react';
import styled, { css } from 'styled-components';

const scope = {
  css,
  styled,
  React,
  useState,
  useRef,
  useEffect,
  useCallback,
  useMemo,
};

export default scope;
