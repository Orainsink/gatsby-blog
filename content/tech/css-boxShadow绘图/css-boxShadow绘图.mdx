---
title: css-boxShadow绘图
date: 2020-12-30 17:04:49
description: 通过css box-shadow可以画出一些非常有趣的图案, css真是博大精深
tags: [css, 性能]
categories: tech
---

> 几个月前在 V2EX 上看到一个一年多经验的小前端说自己 css 无敌，爷当时就笑了。这人呐还是要对技术保持敬畏之心。css 确实从 W3C 规范上看章节不多， 但这积木语言要用好不但需要熟知 API，还得有相应的想象力和创造力。

## 前言

import DemoBtn from './DemoBtn';

<div align="center">
  <DemoBtn />
</div>

这个`buttom`是我在 codePen 上抄的, 源项目地址: [codePen @ashleynolan](https://codepen.io/ashleynolan/pen/wBppKz)

在了解到 `box-shadow` 可以画图之前，对于这种需求我可能选择用 svg 路径动画来制作，但是对于这种图形都是圆圈或者方块的图形，我们可以选择使用 `box-shadow` 绘制。

`box-shadow` 绘图相比 svg 来说其实几乎没用优势，过于复杂的 css 会加长 css 解析的时间，而 css 的加载会阻塞 DOM 树的渲染，所以 `box-shadow` 绘图只能用来玩，是个几乎没有卵用的技能。如果你需要画非常简单的图片的话，还是可以考虑用这个，顺便还可以在同事小伙伴们面前装个逼。

## 实现方式

[MDN box-shadow](https://developer.mozilla.org/en-US/docs/Web/CSS/box-shadow)

```css
selector {
  /* offset-x | offset-y | color */
  box-shadow: 60px -16px teal;

  /* offset-x | offset-y | blur-radius | color */
  box-shadow: 10px 5px 5px black;

  /* offset-x | offset-y | blur-radius | spread-radius | color */
  box-shadow: 2px 2px 2px 1px rgba(0, 0, 0, 0.2);

  /* inset | offset-x | offset-y | color */
  box-shadow: inset 5em 1em gold;

  /* Any number of shadows, separated by commas */
  box-shadow: 3px 3px red, -1em 0 0.4em olive;
}
```

重点看